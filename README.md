
  # Interactive Memory Scrapbook

  This is a code bundle for Interactive Memory Scrapbook. The original project is available at https://www.figma.com/design/Ax3ybJHwtBJpq1G3W6vzWQ/Interactive-Memory-Scrapbook.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  